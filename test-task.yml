#Main yml file for test task Yo!
---
  - hosts: localhost
    become: True
    roles: 
      - kvm-provisioning
      - vm-deployment
      - run-httpd-role
 
   - hosts: testgroup
     become: True
     roles: 
       - run-httpd-role

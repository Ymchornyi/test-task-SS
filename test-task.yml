#Main yml file for test task Yo!
---
  - hosts: localhost
    become: True
    roles: 
      - kvm-provisioning
      - vm-deployment
#    pause:
#      minutes: 1
  
  - hosts: testgroup
    become: True
    roles: 
      - run-httpd-role
